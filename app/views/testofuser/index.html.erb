<table>
  <thead>
    <tr>

     <th>score:</th>

      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @testofuser.each do |testofuser| %>
      <tr>
        <td><%= testofuser.score.to_f.round(2) %></td>
        <td><%= link_to 'Show', testofuser %></td>
        <td><%= link_to 'destroy', testofuser, method: :delete, data: {confirm: 'Are you sure?'}%></td>
      </tr>
    <% end %>
  </tbody>
</table>